# 服务器部署指令

## 最新部署步骤（2026-05-13 23:40）

### 本次修复内容
1. ✅ **修复关键bug**：`app_monitor/views.py` 第395行 `data = [_cache_row_to_dict(row) for row in rows]`
2. ✅ **添加静态文件查找器**：`config/settings.py` 添加 `STATICFILES_FINDERS`
3. ✅ **修复静态文件服务**：`config/urls.py` 区分开发/生产环境
4. ✅ **限制返回数据量**：地图API最多返回10000条记录
5. ✅ **添加缓存机制**：5分钟缓存，减少数据库查询

### 快速重启命令（最常用）

```bash
# SSH连接后执行
ssh root@8.130.88.229

# 快速重启（一行命令）
sudo systemctl restart gunicorn && sudo systemctl restart nginx && echo "✅ 服务已重启"

# 查看状态
sudo systemctl status gunicorn --no-pager | head -n 10
```

### 完整部署命令（代码更新后执行）

```bash
# 1. SSH连接服务器
ssh root@8.130.88.229

# 2. 进入项目目录
cd /home/xuan1203

# 3. 拉取最新代码
git pull

# 4. 激活虚拟环境
source venv/bin/activate

# 5. 收集静态文件（重要！修复logo和geojson 404）
python manage.py collectstatic --noinput

# 6. 重启Gunicorn服务
sudo systemctl restart gunicorn

# 7. 重启Nginx服务
sudo systemctl restart nginx

# 8. 检查服务状态
sudo systemctl status gunicorn
sudo systemctl status nginx

# 9. 查看最近日志（检查是否有错误）
sudo journalctl -u gunicorn -n 50 --no-pager

# 10. 测试API（应该返回HTTP 200）
curl -I https://3.1415926.love/api/map-observations/?start=2024-01-01&end=2026-05-13&protection=all&stats=1
```

### 预期结果
- ✅ 地图API返回HTTP 200（之前是500错误）
- ✅ 静态文件正常加载（logo.png, region_boundary.geojson）
- ✅ 地图点位加载速度提升（限制10000条+5分钟缓存）
- ✅ 多设备登录不再卡顿（DEBUG=False，数据库连接池，Gunicorn 5 workers）

### 性能优化总结
| 优化项 | 修复前 | 修复后 |
|--------|--------|--------|
| DEBUG模式 | True（内存泄漏） | False |
| Gunicorn Workers | 1-3 | 5 |
| 数据库连接 | 每次新建 | 连接池60秒 |
| 地图API返回 | 93032条（42MB） | 10000条（限制） |
| 缓存机制 | 无 | 5分钟缓存 |
| 静态文件 | 404错误 | 正常加载 |

### 故障排查
如果部署后仍有问题：

```bash
# 查看Gunicorn错误日志
sudo journalctl -u gunicorn -n 100 --no-pager | grep -i error

# 查看Nginx错误日志
sudo tail -f /var/log/nginx/error.log

# 测试静态文件
curl -I https://3.1415926.love/static/app_monitor/img/logo.png
curl -I https://3.1415926.love/static/app_monitor/geo/region_boundary.geojson

# 清除Django缓存（如果需要）
python manage.py shell
>>> from django.core.cache import cache
>>> cache.clear()
>>> exit()
```

---

## 服务器信息
- **IP**: 8.130.88.229
- **域名**: 3.1415926.love
- **用户**: root / xuan1203
- **项目路径**: /home/xuan1203
- **虚拟环境**: /home/xuan1203/venv
- **Gunicorn Socket**: unix:/run/gunicorn.sock
- **Gunicorn Workers**: 5
- **数据库**: PostgreSQL with PostGIS
- **缓存记录数**: 93032条（MapObservationCache表）

## Git仓库
- **GitHub**: https://github.com/ANXU-CRAZY/xuan1203.git
- **最新提交**: Fix map observations API bug and add STATICFILES_FINDERS configuration
