# 服务器配置指令 - 修复静态文件 404 问题

## 问题说明
网站返回 200，但静态文件（Leaflet、Font Awesome、Chart.js、jQuery）返回 404。
原因：`settings.py` 缺少 `STATICFILES_DIRS` 配置，Django 无法收集 `static/vendor/` 目录的文件。

## 解决方案

### 方法一：使用自动化脚本（推荐）

```bash
# 1. 登录服务器（使用 root 账户）
# 你已经登录了，跳过此步

# 2. 切换到项目目录
cd /home/xuan1203

# 3. 上传并运行修复脚本
chmod +x fix_static_files.sh
./fix_static_files.sh
```

### 方法二：手动执行（逐步操作）

```bash
# 1. 切换到项目目录
cd /home/xuan1203

# 2. 拉取最新代码（包含 STATICFILES_DIRS 配置）
git pull origin master

# 3. 激活虚拟环境
source venv/bin/activate

# 4. 检查 vendor 文件是否存在
ls -lh static/vendor/

# 5. 如果 vendor 目录为空或不存在，重新下载
python3 download_vendor_assets.py

# 6. 重新收集静态文件（关键步骤）
python manage.py collectstatic --noinput --clear

# 7. 验证文件已复制到 static_root
ls -lh static_root/vendor/

# 8. 设置正确的权限
sudo chown -R www-data:www-data static_root/
sudo chmod -R 755 static_root/

# 9. 重启 Gunicorn
sudo systemctl restart gunicorn

# 10. 检查服务状态
sudo systemctl status gunicorn

# 11. 测试静态文件访问
curl -I https://3.1415926.love/static/vendor/leaflet/leaflet.css
curl -I https://3.1415926.love/static/vendor/font-awesome/all.min.css
curl -I https://3.1415926.love/static/vendor/chart.js/chart.umd.min.js
curl -I https://3.1415926.love/static/vendor/jquery/jquery-3.6.0.min.js
```

## 验证结果

所有 curl 测试应该返回：
```
HTTP/2 200
```

如果返回 404，检查：
1. `static_root/vendor/` 目录是否存在文件
2. Nginx 配置中 `/static/` location 是否正确指向 `/home/xuan1203/static_root/`
3. 文件权限是否正确（www-data 可读）

## 最终测试

访问网站：https://3.1415926.love

打开浏览器开发者工具（F12），检查：
- Network 标签页不应该有 404 错误
- 地图应该正常显示（Leaflet 加载成功）
- 图标应该正常显示（Font Awesome 加载成功）

## 性能优化总结

已完成的优化：
- ✅ DEBUG = False（避免内存泄漏）
- ✅ Gunicorn 5 workers（适配 2 核 CPU）
- ✅ Worker 自动重启（max-requests 500）
- ✅ 数据库连接持久化（CONN_MAX_AGE = 60）
- ✅ 缓存系统（LocMemCache）
- ✅ 本地化 CDN 资源（解决国内访问慢）
- ✅ Nginx 配置修复（socket 路径）
- ✅ 静态文件配置修复（STATICFILES_DIRS）

预期效果：
- 多设备登录不再卡顿
- 页面加载速度提升 50%+
- 静态资源加载稳定（不依赖外部 CDN）
